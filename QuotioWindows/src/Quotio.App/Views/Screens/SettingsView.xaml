<UserControl x:Class="Quotio.App.Views.Screens.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="⚙️" FontSize="28" VerticalAlignment="Center" Margin="0,0,12,0"/>
            <TextBlock Text="Settings" FontSize="24" FontWeight="Bold" VerticalAlignment="Center"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <Border Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Padding="20" Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="Proxy Settings" FontWeight="Bold" FontSize="16" Margin="0,0,0,15"/>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Proxy Port:" VerticalAlignment="Center"/>
                            <TextBox Grid.Column="1" Text="{Binding ProxyPort}" Padding="8" Width="100" HorizontalAlignment="Left"/>
                        </Grid>
                        <CheckBox Content="Auto-start proxy on application launch" IsChecked="{Binding AutoStartProxy}" Margin="0,5"/>
                    </StackPanel>
                </Border>

                <Border Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Padding="20" Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="General" FontWeight="Bold" FontSize="16" Margin="0,0,0,15"/>
                        <CheckBox Content="Start minimized" IsChecked="{Binding StartMinimized}" Margin="0,5"/>
                        <CheckBox Content="Minimize to system tray on close" IsChecked="{Binding MinimizeToTray}" Margin="0,5"/>
                    </StackPanel>
                </Border>

                 <Border Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Padding="20" Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="Notifications" FontWeight="Bold" FontSize="16" Margin="0,0,0,15"/>
                        <CheckBox Content="Enable low quota warnings" IsChecked="{Binding EnableLowQuotaWarnings}" Margin="0,5"/>
                         <Grid Margin="0,10,0,0" IsEnabled="{Binding EnableLowQuotaWarnings}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Warning Threshold (%):" VerticalAlignment="Center"/>
                            <TextBox Grid.Column="1" Text="{Binding LowQuotaThreshold}" Padding="8" Width="60" HorizontalAlignment="Left"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <Button Command="{Binding SaveSettingsCommand}" Content="💾 Save Settings" HorizontalAlignment="Left" Padding="20,10" Background="#673AB7" Foreground="White" BorderThickness="0"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
