<UserControl x:Class="Quotio.App.Views.Screens.ApiKeysView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="🔑" FontSize="28" VerticalAlignment="Center" Margin="0,0,12,0"/>
            <TextBlock Text="API Keys" FontSize="24" FontWeight="Bold" VerticalAlignment="Center"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <Border Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="OpenAI API Key" FontWeight="Bold" FontSize="14"/>
                        <TextBox Margin="0,8,0,0" Padding="10" Text="" />
                        <TextBlock Text="Used for direct OpenAI API access" Foreground="#999" FontSize="11" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>
                <Border Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="Anthropic API Key" FontWeight="Bold" FontSize="14"/>
                        <TextBox Margin="0,8,0,0" Padding="10" Text="" />
                        <TextBlock Text="Used for direct Claude API access" Foreground="#999" FontSize="11" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>
                <Button Content="💾 Save Keys" HorizontalAlignment="Left" Padding="20,10" Background="#673AB7" Foreground="White" BorderThickness="0"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
