<UserControl x:Class="Quotio.App.Views.Screens.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="📊" FontSize="28" VerticalAlignment="Center" Margin="0,0,12,0"/>
            <TextBlock Text="Dashboard" FontSize="24" FontWeight="Bold" VerticalAlignment="Center"/>
            <Button Command="{Binding LoadQuotasCommand}" Content="🔄 Refresh" Margin="20,0,0,0" Padding="10,5"/>
        </StackPanel>

        <!-- Quota Cards -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Quotas}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Width="280" Margin="0,0,15,15" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Padding="16">
                            <StackPanel>
                                <!-- Header -->
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                    <TextBlock Text="☁️" FontSize="20" Margin="0,0,10,0"/>
                                    <TextBlock Text="{Binding ProviderName}" FontSize="16" FontWeight="Bold"/>
                                </StackPanel>

                                <Separator Margin="0,0,0,12" Background="#E0E0E0"/>

                                <!-- Models -->
                                <ItemsControl ItemsSource="{Binding Models}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="0,6">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel>
                                                    <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" FontSize="13"/>
                                                    <ProgressBar Value="{Binding Percentage}" Maximum="100" Height="6" Margin="0,4,10,0" Foreground="#673AB7"/>
                                                </StackPanel>
                                                <TextBlock Grid.Column="1" Text="{Binding FormattedPercentage}" FontWeight="Bold" VerticalAlignment="Bottom" Foreground="#673AB7"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>

                                <!-- Updated At -->
                                <TextBlock Text="{Binding LastUpdated, StringFormat='Updated: {0:g}'}" 
                                           HorizontalAlignment="Right" FontSize="10" Foreground="#999" Margin="0,12,0,0"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
