<UserControl x:Class="Quotio.App.Views.Screens.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="📝" FontSize="28" VerticalAlignment="Center" Margin="0,0,12,0"/>
            <TextBlock Text="Logs" FontSize="24" FontWeight="Bold" VerticalAlignment="Center"/>
            <Button Content="🗑️ Clear" Margin="20,0,0,0" Padding="10,5"/>
        </StackPanel>

        <Border Grid.Row="1" Background="#1E1E1E" CornerRadius="8" Padding="15">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock FontFamily="Consolas" Foreground="#D4D4D4" FontSize="12" TextWrapping="Wrap">
                    <Run Foreground="#6A9955">[INFO]</Run><Run> Application started</Run><LineBreak/>
                    <Run Foreground="#6A9955">[INFO]</Run><Run> Loading settings...</Run><LineBreak/>
                    <Run Foreground="#6A9955">[INFO]</Run><Run> Scanning for installed agents...</Run><LineBreak/>
                    <Run Foreground="#569CD6">[DEBUG]</Run><Run> Found claude.exe at default location</Run><LineBreak/>
                    <Run Foreground="#569CD6">[DEBUG]</Run><Run> Found gemini.exe at default location</Run><LineBreak/>
                    <Run Foreground="#6A9955">[INFO]</Run><Run> Agent scan complete. Found 2 agents.</Run><LineBreak/>
                    <Run Foreground="#DCDCAA">[WARN]</Run><Run> Proxy binary not found, will download on first use</Run><LineBreak/>
                    <Run Foreground="#6A9955">[INFO]</Run><Run> Ready.</Run>
                </TextBlock>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
